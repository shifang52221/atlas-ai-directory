{
  "name": "www12",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "audit:affiliate": "node scripts/audit-affiliate-links.mjs",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:status": "prisma migrate status",
    "db:validate": "node -e \"const { spawnSync } = require('node:child_process'); const env = { ...process.env, DATABASE_URL: process.env.DATABASE_URL || 'postgresql://ai_nav:ai_nav_password@localhost:5432/ai_nav' }; const result = spawnSync('prisma validate', { stdio: 'inherit', env, shell: true }); process.exit(result.status ?? 1);\"",
    "db:deploy": "prisma migrate deploy",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "clsx": "^2.1.1",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "tailwind-merge": "^3.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tailwindcss/postcss": "^4",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "jsdom": "^28.1.0",
    "playwright": "^1.58.2",
    "prisma": "^6.16.2",
    "tailwindcss": "^4",
    "ts-api-utils": "^2.4.0",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
